#include <assert.h>
#include <stdlib.h>
#include <string.h>
#include <stdio.h>
#include <math.h>

#define ARRAY_SIZEOF(a) (sizeof(a) / sizeof(a[0]))
#define ZERO(p, n)      memset(p, 0, n)

typedef long long int lli;

int main(void)
{
#ifndef ONLINE_JUDGE
    freopen("in", "r", stdin);
#endif
    return 0;
}
