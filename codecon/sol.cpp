#include <algorithm>
#include <vector>
#include <cstdio>

#define ARRAY_SIZEOF(a) (sizeof(a) / sizeof(a[0]))
#define ZERO(p, n)      memset(p, 0, n)

using namespace std;

typedef long long int lli;

int main(void)
{
#ifndef ONLINE_JUDGE
    freopen("in", "r", stdin);
#endif
    return 0;
}
