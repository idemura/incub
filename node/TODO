* Error: instead of throw, return 500. Memory leak?
* Session expiration.
* Search people, tags.
* Search suggestions.
* Login (with dynamic test).
* Feed on main page.

- Timestamp in log.

? Overview error hadling!
? Extract schema and refactor SQL.
? Serve object (app). Metadata.
