fn foo(x, y, z: Bool) {
  x;
  x || y;
  x || y && z;
  !x && !y || z;
  x ^^ y && z;
  x || y ^^ z;
  !!x;

  x = y;
  x += y;
  x -= !y || z;

  x = 0o76;
  x = 100;
  x = 0xfa;

  x = a[10];
  x = a[10][b[0]];

  f();
  f(10);
  f(10, 20);
  f(x, 10 && 20);
  a[0](40, 50);
  a[f()];

  return;
  return x;
  return x, !y && z;
}
